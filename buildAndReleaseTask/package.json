{
  "name": "tlstestgate",
  "version": "1.0.0",
  "description": "Build/Release Task to test for non-compliant SSL/TLS settings on public or internal endpoints. Python 3.7 and above is required.",
  "main": "tlstestgate.js",
  "scripts": {
    "test": "mocha -r ts-node/register test/*.ts --reporter xunit --reporter-option output=ResultsFile.xml",
    "lint": "eslint -c buildAndReleaseTask/.eslintrc.js --ext .ts ."
  },
  "author": "Joe Gatt",
  "license": "Apache-2.0",
  "dependencies": {
    "azure-pipelines-task-lib": "^3.2.0",
    "azure-pipelines-tool-lib": "^1.3.0"
  },
  "devDependencies": {
    "@types/mocha": "^8.2.3",
    "@types/node": "^14.18.12",
    "@types/q": "^1.5.5",
    "@typescript-eslint/eslint-plugin": "^5.18.0",
    "@typescript-eslint/parser": "^5.18.0"
  }
}
